{
  "name": "playground",
  "version": "0.1.0",
  "private": true,
  "//": [
    "The library and its five peers (react, react-dom, leaflet, react-leaflet,",
    "@react-leaflet/core) are file: links into the root package, so the playground",
    "and the library share one instance of each. A real consumer declares the",
    "library by git ref and the peers by semver range instead; doing that here",
    "would install second copies and risk React Invalid hook call errors."
  ],
  "dependencies": {
    "@react-leaflet/core": "file:../node_modules/@react-leaflet/core",
    "bootstrap": "^5.1.3",
    "immer": "^9.0.6",
    "leaflet": "file:../node_modules/leaflet",
    "pcic-react-leaflet-components": "file:..",
    "react": "file:../node_modules/react",
    "react-bootstrap": "^2.0.1",
    "react-dom": "file:../node_modules/react-dom",
    "react-leaflet": "file:../node_modules/react-leaflet",
    "react-router-dom": "^5.3.0",
    "use-immer": "^0.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  },
  "scripts": {
    "dev": "vite"
  },
  "engines": {
    "node": "24.x",
    "npm": "12.x"
  }
}
